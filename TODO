current focus
-----------------
saclab=>seislab
fourier/ifourier (multi-component support)
rpdw (clear data before read in)
bug checks (debug and build function testers?)
clean up structure checks (use seischk)
clean up class support
clean up documentation
global undefined values only
add dataless support (npts==0)
finalize multi-component setup


more function ideas (0.5.?)
------------------
get poly (gpoly)
rem poly (rpoly)
get robust poly (grobpoly)
rem robust poly (rrobpoly)
detect glitches (gspikes)
rem glitches (rspikes)
detect offsets (goffsets)
rem offsets (roffsets)

x^n (pown)
n^x (raisen)
e^x (raisee)
log_e x (loge)
log_n x (logn)

abs (abso)
rms (rms)
robust rms (robrms)
sign
real

fir deci (deci2)
 - causal decimation!

freq integration (fint)
freq differentiation (fdif)

fetch record (frec)
- by component
	- one component only
	- files without cmp not returned
	- iftype is ixy
- by header field/value

reverse (flip)

get header definition (ghd)
 - for cleaning up some low level functions
 - inputs should contain necessary parent fields too
 - allow structure output??


more plotting (0.5.1)
-----------------
spectral plotting (pspec1, pspec2, pspec3)
plot choose component (pcmp1, pcmp2, pcmp3)
plot particle motion (multi-component)
picking
deleting


multi-file functions (0.5.2)
---------------------------
rotate (turn)
add, subtr, mul, dvide records
correlate (use mcxc!)
beam
stack
plot versus


true guis (0.5.3)
-------------------------
window
align
pick
delete


header layout change (slated for 0.6.0)
-------------------------------
header partitioned by field type
	- fixes current int64 limitation
	- improves memory usage
	- requires reworked header (structure?)
	- requires significant rewriting of header access functions
	- changing version becomes complicated


add readme (seislab.m) and examples (?.?.?)
-------------------------
	- features
	- tutorials


