current focus
-----------------
bug checks (build function test set)
clean up structure checks (cut checks if calling ch,gh anyways)
clean up class support
clean up documentation
global undefined values only
fix ch (auto field type support)

more function ideas (0.5.?)
------------------
get poly (gpoly)
rem poly (rpoly)
get robust poly (grobpoly)
rem robust poly (rrobpoly)
detect glitches (gspikes)
rem glitches (rspikes)
detect offsets (goffsets)
rem offsets (roffsets)

x^n (pown)
n^x (raisen)
e^x (raisee)
log_e x (loge)
log_n x (logn)

abs (abso)
rms (rms)
robust rms (robrms)
sign
real

fir deci (deci2)

freq integration (fint)
freq differentiation (fdif)

fetch record (frec)
- by component
	- one component only
	- files without cmp not returned
	- iftype is ixy
- by header field/value

reverse (flip)


more plotting (0.5.1)
-----------------
spectral plotting (pspec1, pspec2, pspec3)
plot choose component (pcmp1, pcmp2, pcmp3)
picking
deleting


multi-file functions (0.5.2)
---------------------------
rotate (turn)
add, subtr, mul, dvide records
correlate (use mcxc!)
beam
stack


true guis (0.5.3)
-------------------------
window
align
pick
delete

header layout change (0.6.0)
-------------------------------
header partitioned by field type
	- fixes current npts/nspts limitation
	- requires significant rewriting of header access functions
		- add dataless sac support (npts=0)
		- finalize multi-component setup
	- changing version becomes complicated again


add readme (saclab.m) and examples (?.?.?)
-------------------------
	- features
	- tutorials
	- function list

