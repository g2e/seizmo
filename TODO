current focus
-----------------
bug checks
	- function test scripts
	- test data
clean up warnings/errors
	- right function in identifier
	- how to do group warning operations???
clean up structure checks
clean up class support
clean up documentation
	- examples
	- requirements
	- notes/warnings
add dataless support (npts==0)
	+ rdata,rpdw,cutim support dataless input/output
	- check/update all functions (ick...)
finalize multi-component setup
	+ unused15->ncmp
	+ gh now outputs nan when ncmp unsupported
	- check/update all functions
cutim/rpdw
	- allow more variable input (like: 'b',200,300)
correlate/mcxc
	- clean up and add in

more function ideas (0.5.?)
------------------
get poly (gpoly)
rem poly (rpoly)
get robust poly (grobpoly)
rem robust poly (rrobpoly)
detect glitches (gspikes)
rem glitches (rspikes)
detect offsets (goffsets)
rem offsets (roffsets)

x^n (pown)
n^x (raisen)
e^x (raisee)
log_e x (loge)
log_n x (logn)

abs (abso)
rms (rms)
robust rms (robrms)
sign
real

fir deci (deci2)
 - causal decimation!

freq integration (fint)
freq differentiation (fdif)

fetch record (frec)
- by component
	- one component only
	- files without cmp not returned
	- iftype is ixy
- by header field/value

reverse (flip)

get header definition (ghd)
 - for cleaning up some low level functions
 - inputs should contain necessary parent fields too
 - allow structure output??


more plotting (0.5.1)
-----------------
spectral plotting (pspec1, pspec2, pspec3)
plot choose component (pcmp1, pcmp2, pcmp3)
plot particle motion (multi-component)
picking
deleting


multi-file functions (0.5.2)
---------------------------
rotate (turn)
add, subtr, mul, dvide records
correlate (use mcxc!)
beam
stack
plot versus


guis (0.5.3)
-------------------------
window
align
pick
delete


header layout change (slated for 0.6.0)
-------------------------------
header partitioned by field type
	- fixes int64 limitation (not particularly useful)
	- improves memory usage (8x better for strings)
	- requires reworked header definition (sub-structuring)
	- may require significant rewriting of header access functions
	- changing version may become complicated


add readme (saclab.m) and examples (?.?.?)
-------------------------
	- features
	- tutorials


